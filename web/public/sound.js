// Soft procedural effects; no downloads or audio files. Starts only after a gesture.
window.LanternSound=(()=>{
 let audio=null,master=null,enabled=true,lastStep=0,epoch=0;
 function init(){try{if(!audio){audio=new(window.AudioContext||window.webkitAudioContext)();master=audio.createGain();master.gain.value=.3;master.connect(audio.destination);}if(enabled)audio.resume();}catch{enabled=false;}return enabled;}
 function note(hz,to,seconds=.16,delay=0,type='sine',volume=.2){if(!enabled||!audio||audio.state!=='running')return;const o=audio.createOscillator(),g=audio.createGain(),t=audio.currentTime+delay;o.type=type;o.frequency.setValueAtTime(hz,t);o.frequency.exponentialRampToValueAtTime(Math.max(30,to),t+seconds);g.gain.setValueAtTime(.0001,t);g.gain.exponentialRampToValueAtTime(volume,t+.015);g.gain.exponentialRampToValueAtTime(.0001,t+seconds);o.connect(g);g.connect(master);o.start(t);o.stop(t+seconds+.02);o.onended=()=>{o.disconnect();g.disconnect();};}
 function splash(){if(!enabled||!audio||audio.state!=='running')return;const length=Math.floor(audio.sampleRate*.55),buffer=audio.createBuffer(1,length,audio.sampleRate),data=buffer.getChannelData(0);for(let i=0;i<length;i++)data[i]=(Math.random()*2-1)*Math.min(1,i/(audio.sampleRate*.025))*Math.pow(1-i/length,2);const source=audio.createBufferSource(),filter=audio.createBiquadFilter(),gain=audio.createGain();source.buffer=buffer;filter.type='lowpass';filter.frequency.value=1400;gain.gain.value=.13;source.connect(filter);filter.connect(gain);gain.connect(master);source.start();source.onended=()=>{source.disconnect();filter.disconnect();gain.disconnect();};note(340,125,.24,0,'sine',.1);note(490,260,.23,.11,'sine',.055);}
 function quake(){if(!audio||audio.state!=='running')return;[196,246.94,293.66].forEach((hz,i)=>note(hz,hz,.95,i*.12,'sine',.06));[587.33,739.99,880,739.99,587.33,493.88].forEach((hz,i)=>note(hz,hz*.99,.55,.18+i*.27,'sine',.055));note(130.81,130.81,1.6,0,'sine',.035);}
 function play(kind){if(!enabled)return;if(kind==='quake'&&audio&&audio.state!=='running'){const stamp=epoch;audio.resume().then(()=>{if(enabled&&epoch===stamp&&audio.state==='running')play(kind);}).catch(()=>{});return;}switch(kind){case'quake':quake();break;case'splash':splash();break;case'refill':note(660,880,.28);note(990,1320,.4,.1,'sine',.1);break;case'jump':note(370,650,.15);break;case'double':note(570,970,.18);note(850,1100,.18,.035,'sine',.1);break;case'slide':note(250,110,.2,0,'triangle',.12);break;case'sprint':note(160,320,.16,0,'sine',.08);break;case'death':note(330,220,.45);note(260,170,.55,.12,'sine',.1);break;case'complete':[523,659,784,1046].forEach((n,i)=>note(n,n,.55,i*.13,'sine',.16));break;case'near':note(880,1320,.18,0,'sine',.1);break;case'crow':note(280,220,.12,0,'triangle',.1);break;}}
 return {init,play,toggle(){enabled=!enabled;if(enabled)init();else if(audio)audio.suspend();return enabled;},set(value){enabled=!!value;},get enabled(){return enabled;},suspend(){if(audio)audio.suspend();},resume(){if(enabled&&audio)audio.resume();},step(time,sprint){if(time-lastStep>(sprint?.13:.25)){lastStep=time;note(sprint?165:125,80,.05,0,'sine',.06);}},reset(){lastStep=0;epoch++;}};
})();
